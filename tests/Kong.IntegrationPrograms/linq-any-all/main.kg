use System
use System.Linq

module Demo.LinqAnyAll

fn Main() {
    let numbers: int[] = [1, 2, 3, 4]
    let hasAny = numbers.Any()
    let allPositive = numbers.All((n: int) => n > 0)

    if (hasAny && allPositive) {
        Console.WriteLine(1)
    } else {
        Console.WriteLine(0)
    }
}
